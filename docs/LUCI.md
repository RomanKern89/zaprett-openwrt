# luci-app-zaprett — веб-интерфейс zaprett для OpenWrt

> Версия документа: 1.2 (2026-09-22, контракт v1.3: живая проверка сервисов, монитор, журнал, агрегирующие
> запросы страниц, пошаговая быстрая настройка). Пакет: `packages/luci-app-zaprett` (noarch, MIT). Контракт —
> `docs/ARCHITECTURE.md` v1.3 (§14 главнее §4–§11; §4 UCI, §5 генератор и граница исключений, §6.2 CLI, §6.3 задачи,
> §9 пресеты, §10 автоподбор, §11 LuCI), поля ответов — `docs/BACKEND.md` §6.
> Поддерживаются OpenWrt 24.10 (LuCI 24.10, opkg) и 25.12 (apk).

---

## 1. Состав пакета

```
luci-app-zaprett/
├── Makefile                                          luci.mk, LUCI_DEPENDS:=+luci-base +rpcd-mod-ucode +zaprett
├── htdocs/luci-static/resources/
│   ├── zaprett/common.js                             общий модуль: RPC, загрузка страницы одним запросом, опрос с паузой
│   │                                                 на скрытой вкладке, тексты ошибок и предупреждений, задачи, диалоги
│   ├── zaprett/health.js                             «работает ли обход»: карточки проверки сервисов, монитор и его
│   │                                                 история, список шагов для многошаговых действий
│   └── view/zaprett/{overview,strategies,lists,repo,settings,diagnostics}.js
├── po/templates/zaprett.pot                          msgid на английском (генерируется tests/check_static.py --write-pot)
├── po/ru/zaprett.po                                  полный русский перевод → пакет luci-i18n-zaprett-ru
├── root/etc/uci-defaults/80_luci-zaprett             сброс кэша меню LuCI и reload rpcd (первая загрузка образа)
├── root/usr/share/luci/menu.d/luci-app-zaprett.json  «Службы → zaprett» и 6 подстраниц
├── root/usr/share/rpcd/acl.d/luci-app-zaprett.json   права только на методы luci.zaprett и uci zaprett
├── root/usr/share/rpcd/ucode/luci.zaprett            rpcd ucode-плагин, объект ubus luci.zaprett
└── tests/                                            самопроверки (в пакет не попадают, см. §8)
```

Каталог `tests/` не устанавливается: `luci.mk` копирует только `htdocs`, `root`, `ucode`, `luasrc`, `src`.

---

## 2. Страницы

Меню «Службы → zaprett» (`admin/services/zaprett`, показывается при ACL `luci-app-zaprett` и наличии
`/etc/config/zaprett`).

| Страница | Что делает | Методы |
|---|---|---|
| **Обзор** | Одним запросом `page overview`. **Быстрая настройка** — открыта вверху страницы, пока ничего не настроено (есть предупреждение `no_active_lists` или служба выключена и ни разу не проверялась), иначе свёрнута внизу («выбрать сервисы заново»). Сервисы из пресетов с пометками «zaprett не поможет» (`works=no`), «списки не установлены» (`available=false`), «включено частично», «использует подписки», рекомендация по памяти (`ram_total_mib`, `recommended_tier`) и «не рекомендуется» у `full` на роутере уровня `light`. У сервиса с `variants` (контракт v1.7 §16.4) — «Набор списков»: радиокнопки «Основной» и варианты с названием и описанием на языке интерфейса (`name`/`name_en`, `description`/`description_en`) и оценкой нагрузки по `tier` (`light` — «подходит любому роутеру», `full` — «нужно не меньше N МиБ», на слабом роутере ещё «не рекомендуется»); отмечен текущий по `enabled_variant`, недоступный (`available=false`) заблокирован, вариант с id не по правилу id не показывается. Сервис с включённым вариантом считается включённым (метка «включено», галочка). В `wizard_apply` уходит `id:variant`, для основного набора — просто `id`; проверка сайтов получает только id сервисов; в шаге списков — «Выбраны другие наборы списков: Discord — С голосовыми серверами» по `variants` ответа. Кнопка **«Применить и запустить»** ведёт по шагам с отметками ✓/✗: `wizard_apply` → (ожидание задачи `sources-update`, если мастер её поставил) → `service start` (или `restart`, если служба работала, а `reloaded` не `true`) → пауза 2 с → `probe_start` по выбранным сервисам, у которых есть `test_targets` → ожидание задачи → `probe_status` → карточки. Если сервис не открылся (открылось меньше половины адресов) — «Подобрать рабочую стратегию»: `test_start {quick, apply_if_better}` (старая служба отвечает `usage` → без флага, лучшая применяется `test_apply`), ход задачи, итог из `test_status {brief}` (`results.applied`), повторная проверка сайтов. **Состояние службы**: состояние простыми словами, автозапуск, движок, стратегия, режим, число списков, «Работа движка» (`status.queue.packets` — пакетов через движок), правила nft, WAN, ускорение, версия; кнопки управления и «Проверить настройки». **Предупреждения**: проблемы — жёлтым блоком «На что обратить внимание», `empty_profile_removed`, `strategy_option_ignored`, `test_running` — спокойным блоком «К сведению»; новые коды `ipv6_wan_unhandled`, `low_memory`, `monitor_degraded` объяснены с советом. **«Открываются ли сайты?»** — последний результат `probe status` (карточки «YouTube ✓ 3/3, 420 мс» / «✗ 0/3 — причина»), «Проверить сейчас», «Подробнее» по адресам. **«Монитор доступности»** — состояние (`ok`/`degraded`/`repairing`/`unknown`), последняя проверка, неудачных подряд из порога, интервал, авторемонт и его последний запуск, полоса из 48 цветных ячеек истории (без внешних библиотек, подсказка с временем у каждой). Опрос — один `status` раз в 10 с (задача берётся из `status.job`, монитор перечитывается только при новом `status.monitor.checked_at`), на скрытой вкладке опроса нет. | `page`, `status`, `service`, `check`, `presets`, `wizard_apply`, `probe_start`, `probe_status`, `monitor_status`, `test_start`, `test_status`, `test_stop`, `test_apply`, `job_status` |
| **Стратегии** | Одним запросом `page strategies` (элементы фильтруются по движку в браузере). Текущая стратегия (просмотр текста и аргументов, проверка настроек). Автоподбор: «Быстрая проверка» (рекомендованные) / все установленные / отмеченные в таблице; галочка **«Применить лучшую автоматически»** (`apply_if_better`, текущая стратегия всегда в переборе, применяется только строго лучшая; старая служба — без флага с пояснением); подтверждение, прогресс, отмена, базовый прогон «без обхода», таблица результатов, сообщение о применённой автоматически (`results.applied`), «Применить лучшую» и «Применить» у строки. Ход подбора — `test_status {brief:true}` раз в 3 с (без подробностей по адресам; старая служба — полный ответ), подробности — по кнопке «Подробнее» полным `test_status`. Список установленных стратегий движка с поиском, свои стратегии — «Изменить», «Удалить», редактор (`user-…`, до 64 КиБ, проверка движком). | `page`, `status`, `items`, `set_strategy`, `strategy_show`, `strategy_save`, `strategy_delete`, `check`, `test_start`, `test_status`, `test_stop`, `test_apply`, `service` |
| **Списки** | Режим обработки (`list_mode`, форма UCI, «Применить») с пояснением: исключения действуют в обоих режимах, но исключение по домену не действует там, где сайт узнаётся только по адресу. Вкладки: **Домены** (обходить / не трогать), **IP-сети** (обходить / не трогать) — переключатели сохраняются сразу, число записей, источник и версия, «используется стратегиями», предупреждение о больших списках; редакторы `user-hosts`, `user-hosts-exclude`, `user-ipset`, `user-ipset-exclude` с подсказками (маски `*.`, `http://`, пробелы, кириллические домены) и выводом ошибок бэкенда по строкам. **Подписки** — внешние листы по https-ссылке: «Загружать» (enabled), «Использовать» (включает `src-<имя>` как обычный лист), дата обновления или ошибка, записей и размер, оценка памяти, объём памяти роутера и предупреждение, если включённым подпискам нужно больше 1/8 памяти (или ≥ 5 МиБ на роутере уровня `light`), пометка «слишком много для этого роутера» у больших подписок, «Обновить сейчас», «Обновить включённые», добавление/редактирование/удаление. Загрузка — одним запросом `page lists`. | `page`, `status`, `items`, `toggle_item`, `user_get`, `user_set`, `sources_list`, `sources_update`, `source_save`, `source_delete`, `job_status`, `job_cancel`, `service`, uci |
| **Репозиторий** | Адрес и дата загрузки каталога, «Обновить каталог», «Обновить всё». Поиск, фильтр по типу, «только установленные», «только с обновлениями», выбор нескольких элементов и «Установить выбранные». Группы по типам (стратегии nfqws/nfqws2, списки, исключения, IP-сети, фейковые пакеты, Lua, ByeDPI). Состояние: не установлено / установлено / входит в пакет / есть обновление / не поддерживается на роутере (ByeDPI). «Удалить» — только у установленного из репозитория. Все операции — фоновые задачи с прогрессом и отменой; во время задачи кнопки заблокированы, опрос задачи один (функция остановки хранится, повторная команда не заводит второй опрос). | `repo_list`, `repo_fetch`, `repo_install`, `repo_upgrade`, `repo_remove`, `job_status`, `job_cancel` |
| **Настройки** | `form.Map('zaprett')`. Вкладки секции `main`: «Основные» (запускать, движок, режим, интерфейсы выхода в интернет `widgets.NetworkSelect`, IPv6, ускорение), «Устройства в сети» (режим all/include/exclude, список IPv4/CIDR/MAC с подсказками из DHCP), «Стратегия и списки» (то же, что на страницах, для опытных; текущие значения, которых нет среди установленных, сохраняются и помечаются «не установлено»; если перечень не загрузился — поля только для чтения), «Расширенные» с предупреждением (qnum, метки с проверкой на совпадение и на 0x08000000, лимиты пакетов, пользователь движка, отладка). Секции `repo` (адрес каталога, автообновление, час) и `test` (время ожидания, одновременные запросы 1..16, домены из списков, пауза). Пределы значений не шире, чем в `config.uc` бэкенда (число доменов из списков — до 100 вместо 500, чтобы ответ автоподбора помещался в сообщение ubus). Отсутствующие секции `main/repo/test/monitor` создаются в форме. Секция **«Монитор доступности»** (`monitor.enabled`, `interval` 10/15/20/30/60 мин, `threshold` 1..20, `auto_repair`, `max_targets` 1..20, `timeout` 2..30 с) и **«Сторож движка»** (`main.watchdog`) на вкладке «Основные». Адрес каталога — только `https://` (проверка и текст). | uci, `items`, `luci-rpc getHostHints` |
| **Диагностика** | Состояние, задача и монитор — одним запросом `page diagnostics`; отдельно `diag` и `log`. Диагностический отчёт (`diag`, описание соответствует содержимому быстрого отчёта) — «Обновить», «Скопировать», «Скачать». **Журнал движка** — `log {tail:200}`, «Обновить», «Скопировать» (старая служба — подсказка `logread -e zaprett`). **Подробный журнал движка** — переключатель `uci zaprett.main.debug` с подтверждением о нагрузке; сохраняется через `uci.save()` + `ui.changes.apply(true)` (обычное «Применить» LuCI с откатом), перезапуск делает reload-триггер procd. Монитор доступности (как на «Обзоре»). Последняя фоновая задача: пока идёт — один `job_status` на тик (журнал задачи читается по окончании и по «Обновить»), «Отменить задачу». «Проверить настройки». Описание страницы честно говорит, что в отчёте и журнале есть адреса и имена интерфейсов, настройки zaprett и имена сайтов, но нет паролей роутера. | `page`, `diag`, `log`, `job_status`, `job_log`, `job_cancel`, `check`, uci |

Общие правила интерфейса:

- Весь текст — через `_()` (msgid на английском), перевод — `po/ru/zaprett.po` (672 строки, 100 %).
- Загрузка страниц «Обзор», «Стратегии», «Списки», «Диагностика» — один вызов `page <name>` (`zc.loadPage`): каждое
  поле ответа — ответ соответствующей команды целиком. Если поля нет или весь вызов не удался (служба без `page`
  отвечает `usage`), это поле добирается отдельным вызовом прежнего метода. Любой новый метод, на который старая
  служба отвечает `usage` (`probe`, `monitor`, `log`, `--brief`, `--apply-if-better`), ведёт к запасному поведению, а не к
  ошибке: блок монитора скрывается, проверка сайтов и журнал показывают «обновите пакет zaprett».
- `diag` и `page` объявлены с `nobatch` (есть в `rpc.js` LuCI 24.10 и master): медленный вызов идёт отдельным HTTP-запросом
  и не задерживает быстрые, собранные в пакет вместе с ним.
- Опрос (`zc.pollVisible`, им пользуется и `zc.watchJob`) пропускает тики, пока вкладка скрыта (`document.hidden`), и
  обновляет сразу при возврате (`visibilitychange`); снимается на `pagehide`.
- Двуязычные метаданные (§14.6): при языке интерфейса не `ru*` (`<html lang>` или класс `lang_xx` у `<body>`; без них —
  активен ли наш русский каталог) показываются `name_en`, `description_en`, `note_en`, если они есть, — у сервисов
  пресетов и у элементов (`items`, `repo list`).
- Ячейки таблиц с данными (результаты подбора, стратегии, списки, подписки, репозиторий, адреса проверки) несут
  `data-title` с названием столбца — темы LuCI подписывают по нему ячейки, когда таблица на узком экране
  показывается карточками. Таблицы «параметр — значение» (состояние службы, задача, монитор) — без него: название
  уже в первой ячейке строки.
- Сообщения бэкенда (`message`, по контракту уже на русском) показываются как есть; для кодов уровня веб-интерфейса
  (`backend_missing`, `timeout`, `reply_too_large`, `tmp_unavailable`, …) и транспортных ошибок RPC (нет прав,
  модуль rpcd не загружен, таймаут, HTTP-ошибка) — собственные понятные тексты.
- Кнопки блокируются на время операции (`ui.createHandlerFn`), долгие операции — только фоновые задачи бэкенда
  с опросом через `poll.add`; опрос снимается по окончании задачи, при ошибках связи (5 подряд) и на `pagehide`.
- Отмена задачи: бэкенд отвечает сразу `{state:"cancelling"}` и довершает отмену при следующем опросе
  (`job status`, `test status`, `status`). Интерфейс показывает «Отмена запрошена…», продолжает опрос до
  `state != running` и так же обрабатывает `{state:"restored"}` (задачи нет, снят оставшийся режим автоподбора)
  и таймаут запроса.
- Граница исключений (контракт v1.2 §5): доменные исключения действуют только там, где сайт узнаётся по имени.
  Если тот же сайт попадает во включённый список IP-сетей или подписку, его соединения всё равно обрабатываются —
  интерфейс говорит это прямо и советует добавить адреса в «Исключения IP-сетей».

---

## 3. rpcd-плагин `luci.zaprett`

Файл `/usr/share/rpcd/ucode/luci.zaprett`, загружается rpcd при старте (любой обычный файл каталога, не
доступный на запись всем). Использует только стандартную библиотеку ucode и модуль `fs` — есть в 24.10 и 25.12
(проверено на стендах: `ucode -l fs` и `ucode -c`).

### 3.1. Как вызывается CLI

```
system([ '/bin/sh', '-c', 'in=$1; out=$2; err=$3; shift 3; exec "$@" <"$in" >"$out" 2>"$err"',
         'zaprett-luci', <stdin-файл|/dev/null>, <out-файл>, <err-файл>, '/usr/bin/zaprett', <аргументы…>, '--json' ],
       <лимит> * 1000)
```

- Аргументы передаются массивом (execve) — оболочка их не разбирает; скрипт `sh` постоянный и только
  перенаправляет потоки. `fs.popen()` не подходит: принимает лишь строку для `/bin/sh -c` (проверено: массив →
  `Invalid argument`).
- `exec` заменяет `sh` процессом CLI, поэтому лимит `system()` (SIGKILL) останавливает сам CLI. Вывод читается из
  файла, а не из канала: фоновые процессы бэкенда, унаследовавшие stdout, не задерживают ответ (первая версия на
  `popen` + сторож зависала на 40 с — поймано тестом).
- Временные файлы — в `/var/run/luci-zaprett` (0700, root). Каталог создаётся при первом вызове; если это ссылка,
  он не принадлежит root или доступен группе/другим — `tmp_unavailable`. Файлы удаляются после каждого вызова.
- Текст для `strategy save` / `user set` и JSON для `sources save` пишутся во временный файл и подаются на stdin.
- Лимиты: 15 с на чтение, 19 с на изменения (таймаут RPC в браузере — 20 с); вывод CLI читается до 4 МиБ; ответ
  ubus — не больше 900 000 байт (ограничение сообщения ubus 1 МиБ). Для `test_status` при превышении из
  результатов удаляются `targets` и ставится `targets_trimmed: true`; для `page strategies` — то же внутри поля `test`
  (страховка на случай службы, которая не поддерживает `--brief` внутри `page`).
- Разбор: ответ должен быть JSON-объектом; иначе `{ok:false, error:"backend_error", message:"… exited with code N
  and returned no valid JSON: <начало вывода или stderr>"}`. Если в объекте нет булева `ok`, он выводится из кода
  возврата; при `ok:false` без `error` подставляется `backend_error`, без `message` — stderr.

### 3.2. Методы

rpcd сам проверяет типы аргументов по подсказкам в `args` (строка, bool → INT8, число → INT32, массив) и
отклоняет лишние (`rpcd/ucode.c`, `rpc_ucode_validate_call_args`). Плагин повторяет это сам: аргумент, которого нет в
`args` (кроме `ubus_rpc_session`, его добавляет rpcd), — `invalid_argument`; так плагин строг и при вызове в обход
rpcd (тесты). Значения проверяются дополнительно. id — `^[A-Za-z0-9._-]+$` и длина ≤ 96, имя подписки —
`^[a-z0-9_]+$` и ≤ 32: длина проверяется `length()`, потому что регулярку с `{1,96}` ucode компилирует на каждый
вызов (в ~100 раз медленнее, см. PROJECT_MEMORY §2).

| Метод | Аргументы (проверка) | CLI | Лимит | ACL |
|---|---|---|---|---|
| `status` | — | `status` | 15 с | read |
| `service` | `action` ∈ start, stop, restart, enable, disable | `<action>` | 19 с | write |
| `items` | `type?` ∈ list, list_exclude, ipset, ipset_exclude, nfqws, nfqws2, bin, lua_lib | `items [--type t]` | 15 с | read |
| `toggle_item` | `id`, `enabled` (bool) | `list enable\|disable <id>` | 19 с | write |
| `set_strategy` | `id` | `strategy set <id>` | 19 с | write |
| `strategy_show` | `id` | `strategy show <id>` | 15 с | read |
| `strategy_save` | `id` = `user-…`, `text` (строка ≤ 1 МиБ; бэкенд — ≤ 64 КиБ) | `strategy save <id>` + stdin | 19 с | write |
| `strategy_delete` | `id` = `user-…` | `strategy delete <id>` | 19 с | write |
| `user_get` | `id` = `user-…` | `user get <id>` | 15 с | read |
| `user_set` | `id` = `user-…`, `text` (≤ 1 МиБ) | `user set <id>` + stdin | 19 с | write |
| `check` | — | `check` | 15 с | read |
| `repo_fetch` | — | `repo fetch` (задача) | 15 с | write |
| `repo_list` | `type?` (как у `items` + byedpi) | `repo list [--type t]` | 15 с | read |
| `repo_install` | `ids` (1..100 id, дубли убираются) | `repo install <id>…` (задача) | 15 с | write |
| `repo_remove` | `id` | `repo remove <id>` (задача) | 15 с | write |
| `repo_upgrade` | `ids?` (0..100 id) | `repo upgrade <id>…` или `--all` | 15 с | write |
| `presets` | — | `presets` | 15 с | read |
| `wizard_apply` | `services` (1..32 строк `id` или `id:variant`, обе части по правилу id, v1.7) | `wizard apply <id>[:<variant>]…` | 19 с | write |
| `sources_list` | — | `sources list` | 15 с | read |
| `sources_update` | `names?` (0..64, `^[a-z0-9_]{1,32}$`) | `sources update [<name>…]` (задача) | 15 с | write |
| `source_save` | `name`, `title` (одна строка ≤ 128), `type` ∈ list, list_exclude, ipset, ipset_exclude, `url` (`https://…` ≤ 2048), `interval_hours` 1..8760, `min_entries?` ≥ 0, `enabled` (bool) | `sources save <name>` + JSON на stdin | 19 с | write |
| `source_delete` | `name` | `sources delete <name>` | 19 с | write |
| `test_start` | `strategies?` (0..200 id), `quick?` (bool), `apply_if_better?` (bool) | `test start [--strategies a,b] [--quick] [--apply-if-better]` (задача) | 15 с | write |
| `test_status` | `brief?` (bool) | `test status [--brief]` | 15 с | read |
| `probe_start` | `services?` (0..32 id, дубли убираются) | `probe [--services a,b]` (задача `probe`) | 15 с | write |
| `probe_status` | — | `probe status` | 15 с | read |
| `monitor_status` | — | `monitor status` | 15 с | read |
| `dns_status` | — | `dns status` | 15 с | read |
| `dns_setup` | — | `dns setup` (задача `dns-setup`) | 15 с | write |
| `diagnose_start` | `services?` (0..32 id, дубли убираются) | `diagnose [--services a,b]` (задача `diagnose`) | 15 с | write |
| `diagnose_status` | — | `diagnose status` | 15 с | read |
| `log` | `tail?` 1..1000 (целое) | `log [--tail N]` | 15 с | read |
| `page` | `name` ∈ overview, lists, strategies, diagnostics | `page <name>` | 15 с | read |
| `test_stop` | — | `test stop` | 19 с | write |
| `test_apply` | `id` | `test apply <id>` | 19 с | write |
| `job_status` | — | `job status` | 15 с | read |
| `job_log` | `tail?` 1..5000 (целое) | `job log [--tail N]` | 15 с | read |
| `job_cancel` | — | `job cancel` | 19 с | write |
| `diag` | — | `diag` | 19 с | read |

Коды ошибок самого плагина: `invalid_argument`, `invalid_id`, `backend_missing`, `backend_error`, `timeout`,
`reply_too_large`, `tmp_unavailable`. Остальные коды и сообщения приходят от бэкенда без изменений (вместе с
дополнительными полями: `errors[]` у `user set`, `dry_run` и `args` у `dry_run_failed`, `used_by` и т. п.).

### 3.3. ACL

`luci-app-zaprett`: read — `status, items, strategy_show, user_get, check, repo_list, presets, sources_list,
test_status, job_status, job_log, diag, probe_status, monitor_status, log, page, dns_status, diagnose_status` +
`uci: zaprett`; write — остальные 21 метод (в том числе `probe_start`, `dns_setup`, `diagnose_start`) + `uci: zaprett`. Никакого
`file.exec`, доступа к файлам или интерпретаторам. Страница «Настройки» дополнительно пользуется общими ACL LuCI
(`luci-rpc getHostHints`, сетевые интерфейсы) — у администратора они есть; без них подсказки устройств просто не
показываются.

---

## 4. Безопасность вывода и ограничения LuCI

- **`E()` и `dom.content()` небезопасны для строк.** В LuCI (и 24.10, и master) `dom.append(node, 'строка')`
  выполняет `node.innerHTML = строка`; текстовые узлы создаются только для элементов массива. Поэтому все данные
  с роутера передаются как `[строка]` (`zc.txt()`), заголовки `ui.showModal()`/`ui.addNotification()` — тоже
  массивом. Проверено чтением `luci.js` в обеих ветках и минифицированного `/www/luci-static/resources/luci.js`
  на обоих стендах.
- Логические атрибуты через `E()` задаются `setAttribute`, поэтому `'disabled': false` всё равно отключает
  кнопку — в коде используется `'' | null`.
- **Тело POST к `admin/ubus` не больше 100 КиБ** (`HTTP_MAX_CONTENT = 1024*100` в `/usr/share/ucode/luci/http.uc`
  на 24.10.8 и 25.12.5). Редактор своих списков проверяет размер JSON-строки (≤ 99 КиБ), редактор стратегий — лимит
  бэкенда 64 КиБ. Больший список — копировать на роутер по SSH.
- Ответ ubus ограничен 1 МиБ (`UBUS_MAX_MSGLEN`), см. §3.1.
- rpcd обрабатывает ucode-методы синхронно: пока идёт вызов, остальные запросы LuCI ждут. Все долгие операции —
  задачи бэкенда, отмена отвечает сразу (§2).

---

## 5. Совместимость 24.10 / 25.12

Используемые API сверены с ветками `openwrt-24.10` и `master` репозитория LuCI и с установленными файлами на
стендах (OpenWrt 24.10.8 и 25.12.5): `rpc.declare({…, reject:true})`, `ui.showModal/hideModal`,
`ui.addNotification`, `ui.addTimeLimitedNotification`, `ui.createHandlerFn`, `ui.tabs.initTabGroup`, `poll.add/remove`,
`dom.content`, `form.Map/NamedSection/ListValue/MultiValue/DynamicList/Flag/Value` (`retain`, `optional`,
`datatype`), `tools.widgets.NetworkSelect`, `network.getHostHints()` (`getMACHints`, `getIPAddrByMACAddr`),
`uci.load/get/add/set/save/unload`, `ui.changes.apply` (переключатель подробного журнала), опция `nobatch` у
`rpc.declare` (сверено с веткой `openwrt-24.10`: `rpc.js`, строка 338), `String.prototype.format` (`%m`), элементы
`<details>/<summary>` для свёрнутой быстрой настройки. Ничего из появившегося только в 25.12
(`fs.exec_direct` с прогрессом и т. п.) не используется. JS в стиле LuCI 24.10 (стрелочные функции, `?.`, `??`) —
собственный код LuCI 24.10 написан так же, а `jsmin` из 24.10 понимает шаблонные строки.

---

## 6. Перевод

- msgid — английский, одной строкой, без двойных пробелов (LuCI нормализует пробелы при поиске перевода).
- `po/templates/zaprett.pot` генерирует `tests/check_static.py --write-pot` по тем же правилам, что
  `build/i18n-scan.pl` LuCI: `_('…')` из JS и `title`/`description` из `menu.d`/`acl.d`.
- Русский перевод — `po/ru/zaprett.po` без множественных форм (`N_()` не используется), названия пунктов меню LuCI
  взяты из официального перевода LuCI («Применить», «Межсетевой экран», «Статус → Системный журнал»,
  «Система → Пакеты»).

---

## 7. Соответствие zaprett-app → LuCI

| zaprett-app (Android) | luci-app-zaprett |
|---|---|
| Главная: карточка состояния службы | Обзор → «Состояние службы» с понятным объяснением |
| Кнопки «Запустить / Остановить / Перезапустить сервис» | Обзор: те же кнопки + автозапуск |
| Карточка версий (модуль, nfqws, nfqws2, ciadpi) | Обзор: движок и его версия, версия zaprett; полный список версий — отчёт «Диагностики» |
| Диалог ошибки с «Скопировать лог» | Уведомления с понятным текстом; «Скопировать» / «Скачать» в «Диагностике» |
| «Хосты»: листы доменов с переключателями | Списки → «Домены» (обходить / не трогать) |
| Переключатель «Белый список / Чёрный список» листов | Списки → «Режим обработки» |
| «IP-сети» | Списки → «IP-сети» |
| Импорт своего файла листа с генерацией манифеста | Редакторы своих списков `user-*` и подписки по https-ссылке |
| «Стратегии»: выбор, импорт файла | Стратегии: выбор, просмотр, редактор своих стратегий |
| «Подбор стратегии» (перебор скачанных, домены из листов, таймаут пробы) | Стратегии → «Автоподбор»: фоновая задача, базовый прогон без обхода, быстрый режим, отмена, подробности по адресам, «Применить лучшую» |
| «Репозиторий»: установить / обновить | Репозиторий: + удалить, «Обновить всё», фильтры, пакетная установка, прогресс |
| Репозитории фейков и Lua-библиотек | Репозиторий: типы «Файл фейкового пакета», «Lua-библиотека» |
| Настройки: тип сервиса (nfqws / nfqws2 / byedpi) | Настройки → «Движок» (nfqws / nfqws2) |
| Настройки: автообновление, URL репозитория | Настройки → «Репозиторий и обновления» (+ час обновления) |
| Настройки: списки приложений (белый / чёрный) | Настройки → «Устройства в сети» (IPv4/сеть/MAC) |
| Настройки: «Изменить таймаут пробы» | Настройки → «Автоподбор стратегии» (таймаут, параллельность, число доменов, пауза) |
| Приветствие при первом запуске | Обзор → «Быстрая настройка» по пресетам сервисов: включить → запустить → проверить сайты → при необходимости подобрать стратегию |
| — | Живая проверка сервисов («Проверить сейчас») и монитор доступности с историей и авторемонтом |
| — | Предупреждения о проблемах (нет WAN, ускорение, нет модулей ядра, стратегия не найдена…) |
| — | Подписки на внешние списки (Re:filter, antifilter, Cloudflare, свои) |
| — | Проверка настроек движком (`--dry-run`) с выводом |

---

## 8. Что сознательно не перенесено

| Функция приложения | Почему нет на роутере |
|---|---|
| Режим ByeDPI (ciadpi через VpnService, без root), его IP/порт/DNS, IPv6 для ByeDPI | Это обход через локальный VPN Android. На роутере трафик и так проходит через nfqws; стратегии ByeDPI в каталоге показаны как «не поддерживается на роутере». |
| Плитка быстрых настроек (QS), уведомление «zaprett работает» | Элементы Android. Состояние видно на «Обзоре». |
| Проверка обновлений приложения (`update.json`), диалог обновления | Пакеты обновляются пакетным менеджером из фида (`opkg`/`apk`). |
| «Периодически перезапускать сервис» | procd сам перезапускает движок при падении (`respawn`). |
| Аналитика Firebase | Не нужна и не допустима на роутере. |
| Запросы root / Magisk-модуль / разрешения хранилища и уведомлений | На роутере служба работает от root через procd, разрешения Android не существуют. |
| «Обновлять статус при запуске Главной», «Показывать полный путь», «Показывать системные приложения» | Статус обновляется автоматически раз в 10 с (кроме скрытой вкладки); путей и приложений в интерфейсе нет. |
| Импорт произвольного файла с устройства | LuCI принимает не больше 100 КиБ за запрос; вместо загрузки — редактор своих списков и подписки по ссылке (большие файлы роутер скачивает сам). |
| «Сброс настроек» (раздел «Отладка») | Опасно одним нажатием; настройки меняются формой «Настройки», конфиг `/etc/config/zaprett` восстанавливается переустановкой пакета. |
| URL репозитория обновлений приложения | См. «Проверка обновлений». |

---

## 9. Сверка с бэкендом (2026-09-17, контракт v1.2)

Проверено по исходникам `packages/zaprett` (`commands.uc`, `service.uc`, `sources.uc`, `tester.uc`, `job.uc`,
`repo.uc`, `strategy.uc`) и `docs/BACKEND.md` §6. Настоящий CLI не запускался (тестирование — финальный этап).

Сошлось: все поля, которые читает интерфейс (`status`, `items`, `check`, `strategy show/set/save`, `user get/set`,
`repo list`, `sources list`, `presets`, `wizard apply`, `test status/apply`, `job status/log/cancel`, `diag`),
закрытый список 19 предупреждений, единое поле `reloaded`, коды ошибок и дополнительные поля
(`errors[]`, `dry_run`, `used_by`, `skipped`, `job_error`, `failed[]`).

Приведено в соответствие при сверке:

1. Отмена задач — два шага (`cancelling` → завершение при опросе) и ответ `restored` у `test stop`.
2. Подписки — поля `min_entries`, `status` (`ok|error|never|invalid`), `message`, `downloaded` из `sources list`;
   `min_entries` больше не читается из UCI. `source_save` отправляет только изменённые поля (бэкенд сохраняет
   остальные), поэтому переключатель «Загружать» не перезаписывает адрес и период.
3. Подписку можно включить в работу до первой загрузки (бэкенд это допускает): переключатель «Использовать»
   активен всегда, а состояние объясняет предупреждение `source_not_downloaded`.
4. Мастер: показываются `skipped` (сервисы с `works=no`), включённые и выключенные подписки, `job_error` и
   ход задачи `sources-update`.
5. `profile_unfiltered` — подробности из `details.generate.details.unfiltered_profiles` (в `status`) и
   `details.unfiltered_profiles` (в `check`): номера частей стратегии и их порты.
6. Обрезка результатов автоподбора: бэкенд оставляет подробности у первых 10 результатов (≤ 100 целей),
   интерфейс показывает пометку и по полю `results.targets_trimmed`, и по своему `targets_trimmed` плагина.
7. `items --type byedpi` больше не отклоняется плагином (тип есть в бэкенде).
8. Граница исключений (§5 v1.2) объяснена честно в «Списках» и «Настройках», см. §2.

Осталось на финальный этап (не правится сейчас):

- `diag` у бэкенда собирает список пакетов с таймаутом до 60 с, а лимит вызова из веб-интерфейса — 19 с
  (таймаут RPC в браузере 20 с). На медленном роутере отчёт может не успеть; интерфейс покажет «роутер не успел»,
  отчёт остаётся доступным по SSH. Нужна либо более быстрая сборка отчёта, либо отдельная кнопка «полный отчёт».
- Обрезка `test_status` в плагине оставлена как страховка: при 64 стратегиях и подробностях у 10 из них ответ
  укладывается в сообщение ubus, но при изменении лимитов бэкенда страховка не даст превысить 1 МиБ.

---

## 10. Сверка с контрактом v1.3 (2026-09-22)

Интерфейс написан по §14 `ARCHITECTURE.md` до готовности бэкенда (его делал параллельно другой исполнитель), имена
полей — дословно из контракта. Что интерфейс ждёт от службы и что делает, если этого нет:

| Ждёт | Где | Если нет |
|---|---|---|
| `page <name>` с полями из §14.3 | 4 страницы | поле или весь вызов → прежние отдельные вызовы |
| `status.job`, `status.monitor.checked_at`, `status.queue.packets` | «Обзор» | `job_status` отдельным вызовом; монитор не перечитывается по опросу; строки «Работа движка» нет |
| `probe`, `probe status` (`services[].{id,name,ok,total,avg_ms,targets[]}`, `engine_running`, `strategy`, `started`, `finished`) | «Обзор», мастер | «обновите пакет zaprett»; шаг проверки в мастере пропускается |
| `monitor status` (§14.3) | «Обзор», «Диагностика» | блок монитора не показывается |
| `log --tail N` → `lines[]` | «Диагностика» | подсказка `logread -e zaprett` |
| `test status --brief`, `results.applied` / `job.result.applied` | «Стратегии», мастер | полный `test status`; без `applied` — выбор лучшей и `test_apply` в мастере |
| `test start --apply-if-better` | «Стратегии», мастер | запуск без флага с пояснением |
| `status.job` — последняя задача в любом состоянии (уточнение §14.3) | «Обзор» | баннер задачи — только при `state == running` |
| после `stop` сторож и монитор сняты с расписания (уточнение §14.3) | «Обзор» | в блоке монитора пометка «zaprett не запущен, проверки возобновятся после „Запустить“» — не ошибка |
| цель проверки `error` из закрытого списка (`timeout`, `reset`, `tls_cert`, `tls_error`, `connect_failed`, `http_error`, `too_small`, `local_error`, `failed`) | карточки, подробности | у каждого кода свой текст; неизвестный код показывается как есть |
| коды `ipv6_wan_unhandled`, `low_memory`, `monitor_degraded` | предупреждения | неизвестный код показывается общим текстом «Предупреждение: <код>» |
| `name_en`, `description_en`, `note_en` | пресеты, элементы | исходные поля |

Добавлено в плагин сверх текста §14.7: аргумент `apply_if_better` у `test_start` — это флаг `--apply-if-better` из
§14.3, в §14.7 он не перечислен. Предлагается дописать его в §14.7.

---

## 10a. Сверка с контрактом v1.4 (2026-09-22)

Веб-часть §15.1–§15.5 и §15.7 `ARCHITECTURE.md` написана до готовности бэкенда (его параллельно делал другой
исполнитель); имена полей — дословно из §15. Что интерфейс ждёт от службы и что делает, если этого нет:

| Ждёт | Где | Если нет |
|---|---|---|
| `status.flow_offload.own` (§15.1) | «Настройки», «Обзор» | в «Настройках» нет пункта «своя таблица ускорения», флага «Блокировать QUIC» и игрового фильтра (кроме уже заданных в UCI значений — их видно, чтобы не потерять при сохранении); если `status` не прочитался — всё показывается |
| `main.flow_offload=own`, `main.quic_block`, `main.game_filter`, `main.game_ports_tcp/udp` (§15.1, §15.2) | «Настройки» | — (обычные опции UCI); порты проверяются в браузере: `N` или `N-M` через запятую без пробелов, 1..65535, `N ≤ M` |
| `status.dns = {encrypted, provider}` или поле `dns` в `page overview` (§15.3) | карточка «Шифрованный DNS» на «Обзоре» | карточка не показывается, лишних вызовов нет |
| `dns setup` → задача `dns-setup` или `{ok, changed:false}`; затем `dns status` | кнопка «Включить шифрованный DNS» (карточка, предупреждение `dns_plain`, вывод диагностики) | ошибка показывается в карточке |
| `diagnose`, `diagnose status` (`targets[].{url,host,verdict,dns:{system,doh,spoofed},detail}`, `summary.{verdict,counts}`, `engine_running`, `started`, `finished`) (§15.4) | «Диагностика» → «Чем блокирует провайдер»; мастер — «Выяснить, чем блокирует провайдер» | ответ `usage` → «обновите пакет zaprett»; неизвестный вердикт показывается как есть с советом обновить веб-интерфейс |
| поля `dns`, `diagnose` в `page diagnostics` (§15.7) | «Диагностика» | отдельные вызовы `dns_status`, `diagnose_status` |
| коды `flowtable_failed`, `game_filter_no_ipsets` (предупреждения), `dns_plain` (к сведению) (§15.5) | предупреждения | неизвестный код — общим текстом «Предупреждение: <код>» |
| имена задач `dns-setup`, `diagnose` | баннер задачи, «Диагностика» | имя задачи показывается как есть |

Задача `dns-setup` идёт в карточке DNS (общий баннер фоновой задачи её не дублирует), `diagnose` ведёт ссылкой
на «Диагностику». Если «Диагностика» открыта во время идущей задачи `diagnose`, блок дожидается её результата, а
не запускает новую.

Исправлено по живой проверке волны 1: у значка состояния монитора («Сайты открываются» и др.) пояснение шло
строчным `span.cbi-value-description` со своим `margin-left:.5em`. В теме bootstrap (24.10 и 25.12, снято с
`/www/luci-static/bootstrap/cascade.css` стенда) у этого класса `margin-left:1.25em`, а значок «?» — `::before`
с `position:absolute; left:-1.25em`, поэтому «?» ложился на ярлык. Теперь ярлык — отдельный `inline-block` без
переноса, пояснение — блочный `div` на следующей строке с отступом темы; «?» стоит в собственном поле слева и
не пересекается с ярлыком ни на 1280, ни на 375 px (ширина ярлыка от ширины экрана не зависит). В браузере на
стенде не проверялось. Так же сделаны новые ярлыки (DNS, вердикты).

---

## 11. Самопроверки

| Проверка | Команда | Что доказывает |
|---|---|---|
| Статика | `python tests/check_static.py` | LF и UTF-8; `node --check` каждого JS в обёртке LuCI `(function(window, document, L, …) { … })`; нет `innerHTML`, `eval`, `fs.exec`, `new Function`; `_()` только со строковым литералом; в `.uc`-файлах нет прямого вызова скобочного выражения с `??`, `||`, `&&` или `?:` (ошибка компилятора ucode, подтверждена на 24.10.8 и 25.12.5); JSON валиден; методы плагина = ACL = объявления в `common.js`; пункты меню = файлы видов; `.pot` актуален; `.po` разобран двумя независимыми парсерами (свой и Babel), покрывает все msgid, без пустых и fuzzy, с теми же `%`-подстановками |
| Отрицательные контроли статики | `python tests/negative_controls.py` | 16 намеренно испорченных копий пакета (непереведённая строка, устаревший .pot, подстановки, пустой перевод, дубль в .po, innerHTML, синтаксис, `_()` с переменной, ACL, объявление RPC, меню, CR, четыре формы прямого вызова скобочного выражения в ucode) — каждая должна провалить проверку; плюс 2 положительных контроля (вызов через переменную и скобки без вызова) — проверка обязана их пропустить |
| Имена в JS | `ACORN=<путь к acorn> node tests/check_names.js` | нет необъявленных идентификаторов, `zc.<имя>` существует в `common.js`, `zh.<имя>` — в `health.js`, `createHandlerFn(this, '<метод>')` и `this.<метод>()` ссылаются на существующие методы |
| Сценарии страниц | `node tests/view-smoke.js` | настоящий код страниц на поддельном LuCI (`tests/fakeluci.js`): загрузка одним `page`, отрисовка, один вызов на тик опроса, пауза на скрытой вкладке, перечитывание монитора, запасной путь для старой службы, мастер «применить → запустить → проверить → подобрать → проверить ещё раз» (включая ожидание загрузки подписок), один опрос задачи в «Репозитории», `--brief` и подробности по кнопке, журнал и переключатель отладки, язык метаданных; v1.4: карточка DNS (отмена в диалоге ничего не ставит, установка → ход → итог), новые предупреждения, «Выяснить, чем блокирует провайдер» в мастере и на «Диагностике», слежение за уже идущей диагностикой, проверка игровых портов; v1.7: «Набор списков» у сервиса с вариантами (текущий по `enabled_variant`, недоступный заблокирован, вариант с плохим id не показан, в `wizard_apply` уходит `id:variant`, в проверку сайтов — только id); в каждом сценарии — ни одна строка с роутера не попала в `innerHTML` (данные содержат `<img onerror>`). Если сценарий повис на невыполненном обещании, скрипт завершается с ошибкой, а не молча с кодом 0 |
| Отрицательные контроли сценариев | `python tests/view_smoke_controls.py` | 23 порчи настоящих файлов (строка в `innerHTML`, мастер не запускает службу, подбор без `--apply-if-better`, опрос на скрытой вкладке, язык метаданных, лишние вызовы журнала на тик, мастер не ждёт подписки, «Репозиторий» заводит второй опрос задачи, опрос без `--brief`; v1.4: хост и адреса DNS диагностики строкой в `innerHTML`, `dns_plain` как проблема, установка DNS без подтверждения, карточка DNS у старой службы, перезапуск уже идущей диагностики, порты без проверки порядка, мастер диагностирует все сервисы вместо не открывшихся; v1.7: мастер не передаёт вариант, название варианта строкой в `innerHTML`, текущий вариант не отмечен, сервис с включённым вариантом не считается включённым, id вариантов не проверяются, проверка сайтов получает `id:variant`) — каждая обязана провалить сценарий; файл восстанавливается с проверкой sha256 |
| Плагин + мок CLI | на тестовом OpenWrt: скопировать `luci.zaprett`, `tests/mock-zaprett.uc`, `tests/plugin-test.uc`, `tests/run-plugin-tests.sh` в `/tmp/<каталог>` и выполнить `sh run-plugin-tests.sh /tmp/<каталог>` | подсказки типов аргументов для rpcd, проверка аргументов и отказы, отсутствие shell-инъекций (строки с `'`, `$(…)`, обратными кавычками), точная передача stdin (кириллица, обратные слеши), разбор неверного JSON и stderr, лимиты размера и времени, отсутствие CLI, небезопасный временный каталог (ссылка, 0777), очистка временных файлов, обрезка большого `test_status` (и своя, и пришедшая от бэкенда), два шага отмены задачи, поля подписок v1.2; v1.3 — `page` (4 имени, отказ на 8 неверных), `log` (1..1000, отказ на 0, 1001, строку, дробь), `probe_start/probe_status`, `monitor_status`, `test_status {brief}`, передача `--apply-if-better` в CLI, обрезка большого `page strategies`, отказ на лишний аргумент и пропуск `ubus_rpc_session`. На 24.10.8 и 25.12.5: 198 проверок |

Тесты плагина не трогают rpcd, `/etc`, `/usr` и `/www`: копия плагина переписывается `sed` на мок и временный
каталог внутри тестового каталога, а скрипт проверяет, что реальных путей в копии не осталось.
